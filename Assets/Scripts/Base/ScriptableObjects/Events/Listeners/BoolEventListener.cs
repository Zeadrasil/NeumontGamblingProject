using UnityEngine;

/// <summary>
/// BoolEventListener - A specialized EventListener for boolean values.
/// Listens to a bool-based EventChannel and forwards events to UnityEvents.
/// Inherits core subscription logic from the generic EventListener base class.
/// </summary>
public class BoolEventListener : EventListener<bool>
{
	//
}
