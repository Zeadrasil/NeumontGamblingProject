using UnityEngine;

/// <summary>
/// StringEventListener - A specialized EventListener for string values.
/// Listens to a string-based EventChannel and forwards events to UnityEvents.
/// Inherits core subscription logic from the generic EventListener base class.
/// </summary>
public class StringEventListener : EventListener<string>
{
	//
}
