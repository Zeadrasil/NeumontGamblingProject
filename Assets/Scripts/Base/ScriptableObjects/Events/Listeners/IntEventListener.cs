using UnityEngine;

/// <summary>
/// IntEventListener - A specialized EventListener for integer values.
/// Listens to a int-based EventChannel and forwards events to UnityEvents.
/// Inherits core subscription logic from the generic EventListener base class.
/// </summary>
public class IntEventListener : EventListener<int>
{
	//
}
