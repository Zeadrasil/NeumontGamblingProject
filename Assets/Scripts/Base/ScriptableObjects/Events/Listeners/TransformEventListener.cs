using UnityEngine;

/// <summary>
/// TransformEventListener - A specialized EventListener for Transform values.
/// Listens to a Transform-based EventChannel and forwards events to UnityEvents.
/// Inherits core subscription logic from the generic EventListener base class.
/// </summary>
public class TransformEventListener : EventListener<GameObject>
{
	//
}
